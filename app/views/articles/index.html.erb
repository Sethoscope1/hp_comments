<h1>ARTICLES</h1>

<div ng-controller="ArticleCtrl">
	<h3>Angular Here</h3>
	<ul>
		<li ng-repeat="article in articles">
			{{ article.title }}
		<li>
	</ul>
</div>

<div ng-controller="CommentCtrl">
	<ul>
		<li ng-repeat="comment in comments">
			{{ comment.body }}
		<li>
	</ul>
</div>

<input ng-model="testing" type=text placeholder="Do it">
{{testing}}


<ul>
<% @articles.each do |article| %>
	<li>
		<%= link_to article.title, article_path(article), class: 'article_link' %>	
	</li>
<% end %>


	<li><%= link_to "Create a New Article", new_article_path, class: 'new_article_link' %></li>
</ul>